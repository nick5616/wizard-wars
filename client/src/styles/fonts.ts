/**
 * Whimsical fantasy fonts (Cinzel Decorative + MedievalSharp, loaded via
 * Google Fonts -- see index.html) used app-wide. TITLE_FONT for headlines/
 * section headers/big emphasis text, BODY_FONT for everything else --
 * every overlay sets fontFamily on its own root div (no shared CSS class),
 * so importing these two constants is the only wiring needed per component.
 */
export const TITLE_FONT = "'Cinzel Decorative', serif";
export const BODY_FONT = "'MedievalSharp', cursive";
